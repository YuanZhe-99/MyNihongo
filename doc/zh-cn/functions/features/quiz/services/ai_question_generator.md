# lib/features/quiz/services/ai_question_generator.dart

向设备上的模型索取关于某个单元的额外题目，并拒绝它可能说出的大部分内容。

这里的每一处写法都是为了让一个坏回答不产生任何代价。生成的题带 `QuizQuestion.generated`，因此不进 SM-2 调度器；索取发生在会话**已经**显示之后，所以等待模型永远不会拖慢第一题；每个回复都要先过 `parse` 里的规则才会变成题目。任何一条不过，就静默丢弃——因为没有它，这个会话本来就是完整的。

使用方：`quiz_page.dart`。

**在任何模型被再次问到之前，每道题都先由分析器读一遍**（有分析器时）：`passesParseFilter` 把答案填进空格，要求得到的句子能解析、并且含有被考的语法点，同时要求任何干扰项都不含它。这不花任何代价，并且能在第二次推理被花掉之前，就丢掉那种「空格填什么名词都行」的句子。

**然后每道题都会问两次。** 第一次调用把题写出来；第二次把它**不带**自己给出的答案交回给模型，请模型自己作答、说这道题是否成立，并逐个说明每个选项是否构成正确的句子。只有当模型选到同一个选项、认为它成立、并且只找到一个说得通的选项时，这道题才会被保留。把答案摆在模型面前请它认可，它会认可；所以第二次刻意看不到第一次的答案：两次必须一致的独立推导才是检查，一个橡皮图章不是。而逐项发问，正是抓出「有两个正确答案」的那一半——只被问自己答案的评判永远会放它过去。沉默等于丢弃，和这里其他所有的拒绝一样。

## 声明

| 声明 | 种类 | Tier | 用途 |
|---|---|---|---|
| `AiQuestionGenerator` | 类 | B | 索取关于某单元的额外题目。 |
| `AiQuestionGenerator.new` | 构造函数 | B | 保存单元、目录、提示词构建器、语言、服务与分析器。 |
| [`generate`](#generate) | 方法 | A | 题目到达即产出。 |
| `_words` | getter | B | 该单元的词，用来给提示词落地。 |
| [`parse`](#parse) | 静态方法 | A | 把一个模型回复变成题目，或者拒绝它。 |
| `accepts` | 静态方法 | B | 一道被判定过的题能否出现：答案一致、被判为成立、且只有一个选项说得通并正是答案。 |
| [`passesParseFilter`](#passesparsefilter) | 静态方法 | A | 在再次询问模型之前，用分析器检查这道题。 |
| `_blank` | 静态字段 | B | 生成题用来标记空格的那个记号。 |
| `_survivesReview` | 方法 | B | 让模型回答它自己出的题、作出判定，并逐项评定选项。 |
| `_after` | 静态方法 | B | 取一行里标签之后的内容。 |

## 文档

### `Stream<QuizQuestion> generate({int? limit, Set<String> avoid})` <a id="generate"></a>

- **种类：** 方法
- **用途：** 逐个生成题目，到达即产出。
- **输入：** `limit`——索取几道，缺省用资源里的 `maxQuizQuestions`；`avoid`——会话里已有的题面。
- **返回：** 已接受题目的流。
- **副作用：** 每尝试一个语法点，就在设备上跑一到两次模型。
- **算法：** 按顺序遍历单元的语法点。对每一个构建提示词，交给 `AiPracticeService.runInBackground`（它会给交互请求让路并稍后重试），解析回复；题面不新就丢弃；给了分析器时先过 `passesParseFilter`，再过模型评判，活下来的才产出。到 `limit` 停止。
- **用法：** `quiz_page._generate`，在会话建好之后用 `unawaited` 启动。
- **备注：** 用流而不是列表，因为每道题一存在就有用：会话把它追加进去，学习者可能在下一道还在写的时候就答到了它。`avoid` 由已抽出的题面初始化，所以生成题不会重复题库确定性产出的那些。

### `static QuizQuestion? parse(String raw, {required GrammarPoint point})` <a id="parse"></a>

- **种类：** 静态方法
- **用途：** 把一个模型回复变成题目，或者拒绝它。
- **输入：** 回复 `raw` 和被问的语法点 `point`。
- **返回：** `QuizQuestion?`——只要有一处不对就是 null。
- **副作用：** 无。
- **算法：** 在各行里找 `Q:`、`A:`–`D:`、`Answer:` 和 `Why:`，半角全角冒号都认。然后是五类拒绝，每一类挡住一种具体故障：

  | 拒绝条件 | 原因 |
  |---|---|
  | 没有 `Q:` 行，或它为空 | 模型用散文作答 |
  | 句子里没有 `＿` 或 `_` | 什么也没在问 |
  | 选项不是四个 | 不是四选一 |
  | 有空选项 | 界面上会出现空白选项 |
  | 两个选项相同 | 两个正确答案 |
  | `Answer:` 指向不存在的选项 | 回复自相矛盾 |

- **用法：** `generate`；在 `ai_question_generator_test.dart` 里直接测试。
- **备注：** 这些都不能靠猜来修补，而**猜出来的题比没有题更糟**，因为在屏幕上它看起来和手写的题一样权威。测验界面在生成题上方显示的标注是这件事的另一半：见 [`ai-assist.md`](../../../../features/ai-assist.md)。

### `static bool passesParseFilter(QuizQuestion question, {required GrammarPoint point, required SentenceAnalysis Function(String) analyze})` <a id="passesparsefilter"></a>

- **种类：** 静态方法
- **用途：** 在再次询问任何模型之前，用分析器检查这道生成的题。
- **输入：** 解析好的 `question`、它声称在考的 `point`，以及 `analyze`——句子分析器自己的入口。
- **返回：** `bool`——是否值得再花一次模型调用。
- **副作用：** 无。
- **算法：** 依次把每个选项填进空格，再读结果：

  | 拒绝条件 | 原因 |
  |---|---|
  | 答案所在的句子含未知词 | 这样的句子应用事后也解释不了 |
  | 答案所在的句子不含这个语法点 | 归在 〜ね 之下却没有 〜ね 的题，考的是别的东西 |
  | 某个干扰项所在的句子含这个语法点 | 那个选项是第二个正确答案 |

  若某个语法点的 `effectiveMatchForms` 为空——所有单字助词都是如此，因为这么短的形式几乎会匹配目录里的每一句——这里无法判定，就只用未知词那一条，其余交给模型评判。干扰项解析不了没有关系：错的选项本来就可以是不成话的，那正是它错的原因。
- **用法：** `generate`，在给了 `analyze` 时；`quiz_page._generate` 从 `sentenceAnalyzerProvider` 取它，取不到就传 null，于是分析器加载失败的会话照样有题，只是像这个机制存在之前那样、单由模型来判。
- **备注：** 这里检查的每一件事都是应用早已知道的，而它排在最前面，是因为它不花钱。它在设备上丢掉的那道题是「わたし＿＿が学生です。」，选项为 私、友達、先生、日本語：四个名词，一个填谁都行的空格，以及一个句子里根本没有的语法点。
